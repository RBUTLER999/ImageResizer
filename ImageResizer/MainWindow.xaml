<Window x:Class="ImageResizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="200" Width="500">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <Label>Source directory</Label>
        <TextBox Grid.Column="1" AcceptsReturn="True" Margin="0,0,0,10" Name="txtSource"/>
        <Label Grid.Row="1">Destination directory</Label>
        <TextBox Grid.Row="1" Grid.Column="1" AcceptsReturn="True" Name="txtDesitination"/>
        <StackPanel Grid.Row="2" Grid.Column="1" HorizontalAlignment="Right">
            <Button Click="Button_Click">Go</Button>
        </StackPanel>
    </Grid>
</Window>
